#!/bin/bash
# shellcheck disable=SC1091
if [[ -f .env ]]; then
  dotenv .env
fi

export REPO=$PWD
export VIRTUAL_ENV=$REPO/.venv
path_add PATH $VIRTUAL_ENV/bin
if [ ! -d "$VIRTUAL_ENV" ]; then
  printf >&2 "\033[33mwarning: creating virtualenv for the first time\n\n"
  uv sync
fi
